{
  "name": "careersync-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "node startup-helper.js",
    "backend": "npm --prefix backend run dev",
    "frontend": "npm --prefix frontend start",
    "seed:all": "npm --prefix backend run seed:all",
    "seed:job-matching": "npm --prefix backend run seed:job-matching",
    "seed:comprehensive": "npm --prefix backend run seed:comprehensive",
    "install:all": "npm --prefix backend install && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "@fontsource/inter": "^5.2.8",
    "react-icons": "^5.6.0"
  }
}
